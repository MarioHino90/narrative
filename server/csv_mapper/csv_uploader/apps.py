from django.apps import AppConfig

class CSVUploaderConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'csv_uploader'
    verbose_name = 'CSV Uploader Application'
